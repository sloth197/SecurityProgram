<UserControl x:Class="SecurityProgram.App.Views.EventLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <!--Title (EventLog)-->
        <TextBlock Text = "이벤트로그 모니터링"
                   FontSize = "24"
                   FontWeight = "Bold"
                   Margin = "0,0,0,15" />
        <DataGrid ItemSource = "{Binding Events}"
                  AutoGenerateColumns = "False"
                  GridLinesVisibility = "None"
                  CanUserAddRows = "False" >
        <!-- Add Filter status-->
        <TextBlock Text = "{Binding FilterInfo}"
                   FontSize = "12"
                   Foreground = "LightGray"
                   Margin = "0,0,0,10" />
        <StackPanel Orietation = "Horizontal"
                    Margin = "0,0,10,0">
            <CheckBox Content = "Error"
                      IsChecked = "{Binding ShowError}"
                      Margin = "0,0,10,0" />
            <CheckBox Content = "Warning"
                      IsChecked = "{Binding ShowWarning}"
                      Margin = "0,0,10,0" />
            <CheckBox Content = "Failure"
                      IsChecked = "{Binding ShowFailure}"
                      Margin = "0,0,10,0" />
        </StackPanel>

            <DataGridColumns> 
                <!--시간 데이터를 화면에 표시-->
                <DataGridTextColumn Header = "Time" 
                                    Binding = "{Binding TimeGenerated}"
                                    Width = "160" />
                <!--소스데이터를 화면에 표시-->
                <DataGridTextColumn Header = "Source" 
                                    Binding = "{Binding Source}"
                                    Width = "130" />
                <!--유형 데이터를 화면에 표시-->
                <DataGridTextColumn Header = "Type" 
                                    Binding = "{Binding EntryType}"
                                    Width = "100" />
                <!--메세지 데이터를 화면에 표시-->
                <DataGridTextColumn Header = "Message" 
                                    Binding = "{Binding Message}"
                                    Width = "*" />
            </DataGridColumns>
        </DataGrid>
    </Grid>
</UserControl>