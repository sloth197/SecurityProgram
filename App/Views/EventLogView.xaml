<UserControl x:Class="SecurityProgram.App.Views.EventLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Event Log Monitoring"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <CheckBox Content="Error"
                      IsChecked="{Binding ShowError}"
                      Margin="0,0,12,0" />
            <CheckBox Content="Warning"
                      IsChecked="{Binding ShowWarning}"
                      Margin="0,0,12,0" />
            <CheckBox Content="Failure"
                      IsChecked="{Binding ShowFailure}" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="None"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding TimeGenerated}"
                                    Width="170" />
                <DataGridTextColumn Header="Source"
                                    Binding="{Binding Source}"
                                    Width="150" />
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding EntryType}"
                                    Width="120" />
                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="*" />
                <DataGridTextColumn Header="Event ID"
                                    Binding="{Binding EventId}"
                                    Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="{Binding FilterInfo}"
                       FontSize="12"
                       Foreground="DimGray" />
            <TextBlock Text="{Binding AlertMessage}"
                       FontSize="12"
                       Foreground="DarkRed"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
