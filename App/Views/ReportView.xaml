<UserControl x:Class="SecurityProgram.App.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Security Report Draft"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0"
                     Grid.Column="0"
                     Margin="0,0,10,8"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding ReportTitle, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding AnalystName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Grid.Row="1"
                     Grid.Column="0"
                     Margin="0,0,10,0"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding TargetSystem, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding ExecutiveSummary, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Generate Preview"
                    Width="140"
                    Margin="0,0,8,0"
                    Command="{Binding GeneratePreviewCommand}" />
            <Button Content="Copy Preview"
                    Width="110"
                    Margin="0,0,12,0"
                    Command="{Binding CopyPreviewCommand}" />
            <TextBlock VerticalAlignment="Center"
                       Foreground="DimGray"
                       Text="{Binding LastGeneratedText}" />
        </StackPanel>

        <TextBox Grid.Row="3"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Text="{Binding ReportPreview}" />
    </Grid>
</UserControl>
